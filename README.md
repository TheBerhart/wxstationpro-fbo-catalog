# WxStationPro FBO Catalog (public read-only mirror)

Static thin-seed export for Phase 1 spike. Source of truth remains private `TheBerhart/WxStationPro` (`docs/v1` + `fbo-data/build-catalog-api.js`).

No write keys. No reporter PII. `schema_version` on responses.

Example: `/v1/airports/KAPA/businesses.json`
